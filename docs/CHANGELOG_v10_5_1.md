# CHANGELOG v10.5.1

- Cambiado “snapshot/snap/fallback” en la interfaz por “datos base locales”.
- Añadido `iniciar_web.py` y `INICIAR_WEB.bat` para servir la web sin trazas molestas cuando el navegador corta una descarga de audio.
- Audio con `preload=metadata` para reducir descargas de MP3 innecesarias.
- Textos de orderbooks aclarados: datos base locales hasta que se actualicen con orderbooks live.
